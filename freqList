male.tags :
	grep -w MALE *.gender \
     |  grep -ow '#[[:alpha:]]*' |tr [A-Z] [a-z] |sort |uniq -c |sort -r > $@

female.tags :
	grep -w FEMALE *.gender \
     |  grep -ow '\#[[:alpha:]]*' |tr [A-Z] [a-z] |sort |uniq -c |sort -r > $@
